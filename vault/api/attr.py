from impl.attr import node_create as n_create
from impl.attr import edge_create as e_create

def node_create(project, name): 
  n_create(project, name)

def edge_create(project, name): 
  e_create(project, name)
